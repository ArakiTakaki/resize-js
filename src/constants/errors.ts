export const inputFileNotFoundException = (fileName: string) => `ファイルが存在していません${fileName}`;

/**
 * 出力ファイルエラー
 */
export const outputTargetNotFoundException = (fileName: string) => `出力先ファイルが存在していません ${fileName}`;

